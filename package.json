{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "install": "lerna bootstrap",
    "test": "lerna exec yarn test:cypress --stream"
  },
  "workspaces": {
    "packages": [
      "cli",
      "packages/*",
      "npm/*"
    ],
    "nohoist": [
      "**/@ffmpeg-installer",
      "**/@ffmpeg-installer/**",
      "**/webpack-preprocessor/babel-loader",
      "**/webpack-batteries-included-preprocessor/ts-loader",
      "**/cypress-circleci-reporter"
    ]
  }
}
